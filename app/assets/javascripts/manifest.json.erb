<% icon_sizes = Rails.configuration.serviceworker.icon_sizes %>
{
  "name": "NowOnSale - マンガの新刊発売情報をお届け",
  "short_name": "NowOnSale",
  "start_url": "/",
  "icons": [
  <% icon_sizes.map { |s| "#{s}x#{s}" }.each.with_index do |dim, i| %>
    {
      "src": "<%= image_path "app-icon/#{dim}.png" %>",
      "sizes": "<%= dim %>",
      "type": "image/png"
    }<%= i == (icon_sizes.length - 1) ? '' : ',' %>
  <% end %>
  ],
  "theme_color": "#58b70f",
  "background_color": "#FFFFFF",
  "display": "fullscreen",
  "orientation": "portrait"
}
